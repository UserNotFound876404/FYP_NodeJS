<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title><%= typeof title !== 'undefined' ? title : (typeof appName !== 'undefined' ? appName + ' — Home' : 'Hospital System — Home') %></title>
  <style>
    :root{
      --bg: #f6f8fb;
      --card: #ffffff;
      --accent: #2b7de9;
      --muted: #6b7280;
      --glass: rgba(43,125,233,0.08);
      font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, Arial;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      min-height:100vh;
      background:linear-gradient(180deg,var(--bg),#eef6ff 100%);
      color:#0f172a;
      display:flex;
      justify-content:center;
      padding:28px 20px;
    }
    .wrap{width:100%;max-width:1100px}
    /* Top menu bar */
    .menubar{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:12px;
      margin-bottom:20px;
    }
    .brand{display:flex;gap:12px;align-items:center}
    .logo{
      width:52px;height:52px;border-radius:10px;background:var(--accent);
      color:white;font-weight:700;font-size:28px;display:flex;align-items:center;justify-content:center;
      box-shadow:0 8px 28px rgba(43,125,233,0.12);
    }
    nav.menu{display:flex;gap:6px;align-items:center}
    nav.menu a{
      text-decoration:none;
      color:var(--muted);
      font-weight:700;
      padding:8px 12px;
      border-radius:8px;
      transition:background .12s, color .12s;
    }
    nav.menu a:hover{background:#f0f6ff;color:var(--accent)}
    nav.menu a.primary{background:var(--accent);color:white;box-shadow:0 6px 18px rgba(43,125,233,0.08)}
    /* Responsive collapse button (simple) */
    .menu-toggle{
      display:none;
      background:transparent;border:0;font-size:18px;color:var(--muted);cursor:pointer;
    }

    main{display:grid;grid-template-columns:1fr 340px;gap:18px;align-items:start}
    .card{background:var(--card);border-radius:12px;padding:18px;box-shadow:0 6px 18px rgba(15,23,42,0.06)}
    .hero{display:flex;flex-direction:column;gap:12px}
    .lead{font-size:18px;margin:0}
    .muted{color:var(--muted);font-size:14px;margin:0}
    .features{display:grid;grid-template-columns:repeat(2,1fr);gap:12px;margin-top:12px}
    .feature{background:var(--glass);padding:12px;border-radius:10px;color:var(--accent);font-weight:700;display:flex;gap:10px;align-items:center}
    .feature .desc{font-weight:600;color:#0f172a}
    .callout{margin-top:14px;padding:12px;border-radius:10px;background:linear-gradient(180deg,#fff,#fbfdff);border:1px solid #eef5ff}
    .actions{display:flex;gap:10px;margin-top:12px;flex-wrap:wrap}
    .btn{display:inline-flex;align-items:center;gap:8px;padding:10px 14px;border-radius:10px;border:0;background:var(--accent);color:white;font-weight:700;cursor:pointer;text-decoration:none}
    .btn.ghost{background:transparent;color:var(--accent);border:1px solid rgba(43,125,233,0.12)}
    footer{margin-top:18px;color:var(--muted);font-size:13px;text-align:center}
    ul{margin:8px 0 0 18px;padding:0;color:var(--muted)}
  </style>
</head>
<body>
  <div class="wrap">
    <header class="menubar">
      <div class="brand" aria-label="Hospital System">
        <div>
          <div style="font-weight:900">Hospital System</div>
          <div style="color:var(--muted);font-size:13px">Secure patient medicine listing</div>
        </div>
      </div>

      <button class="menu-toggle" id="menuToggle" aria-expanded="false" aria-controls="mainMenu">☰</button>

      <nav class="menu" id="mainMenu" role="navigation" aria-label="Main menu">
        <a href="/help">Help</a>
        <a href="/about">About</a>
        <a href="/signup">Signup</a>
        <a href="/list">List</a>
        <a href="/login" class="primary">Login</a>
      </nav>
    </header>

    <main>
      <section class="card hero" aria-labelledby="hero-heading">
        <h1 id="hero-heading" style="margin:0;font-size:20px">What this site does</h1>

        <p class="lead">A minimal hospital dashboard that stores the list of patients who require medicines. Patient personal details and required medicine records.</p>

        <p class="muted">To view or manage the patient list, sign in first at <code>/login</code>. The homepage describes the system and provides access links.</p>

        <div class="features" aria-hidden="false">
          <div class="feature">
            <div style="font-size:20px">🩺</div>
            <div>
              <div class="desc">Patient-focused</div>
              <div class="muted">View each patient's basic personal information and medicines they need.</div>
            </div>
          </div>

          <div class="feature">
            <div style="font-size:20px">🔒</div>
            <div>
              <div class="desc">Secure by design</div>
              <div class="muted">Access to database requires authentication.</div>
            </div>
          </div>

          <div class="feature">
            <div style="font-size:20px">📄</div>
            <div>
              <div class="desc">Support CRUD features</div>
              <div class="muted">The website include create, read, update and delete functions.</div>
            </div>
          </div>

          <div class="feature">
            <div style="font-size:20px">⚙️</div>
            <div>
              <div class="desc">API available</div>
              <div class="muted">The system can be connected through API</div>
            </div>
          </div>
        </div>

        <div class="callout" role="region" aria-label="How it works">
          <strong>How it works</strong>
          <ul>
            <li>The homepage describes the application and shows navigation.</li>
            <li>Users sign in at <code>/login</code> to authenticate.</li>
            <li>Sign up if you dont have a account</li>
            <li>After signing in, open <code>/list</code> to view/manage the patient medicine list.</li>
          </ul>
        </div>

        <div class="actions">
          <a class="btn" href="/login">Sign in</a>
          <a class="btn ghost" href="/help">Help</a>
        </div>
      </section>

      <aside class="card" aria-labelledby="summary-heading">

      </aside>
    </main>

    <footer>© Simple Hospital System — S381F group project · Version 1.0</footer>
  </div>
</body>
</html>