<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Help page</title>
  <style>
    :root {
      --page-bg: #f6f8fb;
      --card-bg: #ffffff;
      --accent: #2b7de9;
      --muted: #6b7280;
      --radius: 10px;
      --shadow: 0 8px 24px rgba(15, 23, 42, 0.06);
      --gap: 10px;
      --content-width: 920px;
      font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    }

    html,body { height: 100%; margin: 0; background: linear-gradient(180deg,var(--page-bg),#eef6ff); color: #0f172a; }

    /* Page container centers content and constrains width */
    .page {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 28px 20px;
      gap: 18px;
    }

    /* The content column where menubar + card live (centered) */
    .content {
      width: 100%;
      max-width: var(--content-width);
      display: flex;
      flex-direction: column;
      gap: 18px;
      align-items: center; /* center children horizontally */
    }

    /* Header / menubar that is centered inside .content */
    header.menubar {
      width: 100%;
      display: flex;
      align-items: center;
      justify-content: center;  /* center menu horizontally */
      gap: 12px;
    }

    /* Inner container to limit menubar width and align with card */
    .menubar-inner {
      width: 100%;
      max-width: 720px; /* controls how wide the menu row is */
      display:flex;
      align-items:center;
      justify-content:space-between; /* brand left, menu right inside inner */
      gap:12px;
    }

    .brand {
      display:flex;
      gap:12px;
      align-items:center;
    }

    .logo {
      width: 44px;
      height: 44px;
      border-radius: 10px;
      background: var(--accent);
      color: white;
      font-weight: 700;
      font-size: 20px;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 8px 28px rgba(43, 125, 233, 0.12);
    }

    nav.menu {
      display: flex;
      gap: 8px;
      align-items: center;
    }

    nav.menu a {
      text-decoration: none;
      color: var(--muted);
      font-weight: 700;
      padding: 8px 12px;
      border-radius: 8px;
      transition: background .12s, color .12s;
    }

    nav.menu a:hover { background: #f0f6ff; color: var(--accent); }
    nav.menu a.primary { background: var(--accent); color: white; box-shadow: 0 6px 18px rgba(43,125,233,0.08); }

    /* Routes card (centered under menu) */
    .routes-card {
      background: linear-gradient(180deg, var(--card-bg), #fbfdff);
      border-radius: var(--radius);
      padding: 14px;
      box-shadow: var(--shadow);
      border: 1px solid rgba(15, 23, 42, 0.03);
      width: 100%;
      max-width: 720px; /* matches menubar-inner for visual alignment */
    }

    .routes-title {
      font-weight: 700;
      margin-bottom: 8px;
      font-size: 15px;
      color: #0f172a;
    }

    .routes-list {
      color: var(--muted);
      font-size: 13px;
      line-height: 1.45;
      display: grid;
      gap: 8px;
    }

    .routes-list strong {
      color: #0b1220;
      font-weight: 700;
      display: inline-block;
      min-width: 80px;
    }

    .routes-list code {
      background: rgba(15, 23, 42, 0.03);
      padding: 2px 6px;
      border-radius: 6px;
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, "Roboto Mono", "Courier New", monospace;
      font-size: 12px;
      color: #0b1220;
    }

    footer { font-size: 13px; color: var(--muted); margin-top: 6px; }

    /* Responsive: stack brand above menu on narrow screens */
    @media (max-width:720px) {
      .menubar-inner { max-width: 100%; flex-direction: column; align-items: center; gap: 10px; }
      nav.menu { flex-wrap: wrap; justify-content: center; }
      .routes-card { max-width: 100%; }
    }
  </style>
</head>
<body>
  <div class="page">
    <div class="content">

      <header class="menubar" role="banner">
        <div class="menubar-inner">
          <div class="brand" aria-label="Hospital System">
            <div>
              <div style="font-weight:900">Hospital System</div>
              <div style="color:var(--muted);font-size:13px">Secure patient medicine listing</div>
            </div>
          </div>

          <nav class="menu" id="mainMenu" role="navigation" aria-label="Main menu">
            <a href="/">Home</a>
            <a href="/about">About</a>
            <a href="/signup">Signup</a>
            <a href="/list">List</a>
            <a href="/login" class="primary">Login</a>
          </nav>
        </div>
      </header>

      <main>
        <div class="routes-card" role="region" aria-labelledby="routes-heading">
          <div id="routes-heading" class="routes-title">Main routes</div>

          <div class="muted routes-list">
            <div><strong>/help</strong> — Documentation</div>
            <div><strong>/about</strong> — About the hospital/system</div>
            <div><strong>/signup</strong> — Register an account</div>
            <div><strong>/login</strong> — Authenticate</div>
            <div><strong>/list</strong> — (Protected) Patient medicine list — requires <code>/login</code></div>
          </div>
        </div>
      </main>

      <footer>© Simple Hospital System</footer>
    </div>
  </div>
</body>
</html>