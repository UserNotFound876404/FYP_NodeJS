<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Action success</title>
  <style>
    :root{
      --bg: #f6f8fb;
      --card: #ffffff;
      --success: #16a34a;      /* green */
      --accent: #2b7de9;       /* blue */
      --muted: #6b7280;
      --radius: 12px;
      --shadow: 0 10px 30px rgba(15,23,42,0.06);
      font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    }

    /* Page layout */
    html,body{height:100%;margin:0;background:linear-gradient(180deg,var(--bg),#eef6ff);color:#0f172a}
    .wrap{
      min-height:100vh;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:28px 20px;
    }

    /* Success card */
    .success-card{
      background: linear-gradient(180deg,var(--card),#fbfdff);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      padding: 22px;
      max-width:640px;
      width:100%;
      border:1px solid rgba(15,23,42,0.04);
      display:flex;
      gap:18px;
      align-items:center;
    }

    /* Icon column */
    .icon {
      width:72px;
      height:72px;
      min-width:72px;
      border-radius:14px;
      background: linear-gradient(180deg, rgba(22,163,74,0.12), rgba(22,163,74,0.06));
      display:flex;
      align-items:center;
      justify-content:center;
      color:var(--success);
      font-size:32px;
      font-weight:800;
      box-shadow: 0 8px 22px rgba(22,163,74,0.06);
    }

    /* Content column */
    .content{
      flex:1;
    }

    h1{
      margin:0 0 6px 0;
      font-size:20px;
      letter-spacing:-0.2px;
    }

    p.message{
      margin:0 0 14px 0;
      color:var(--muted);
      font-size:14px;
      line-height:1.45;
    }

    /* Actions */
    .actions{
      display:flex;
      gap:12px;
      flex-wrap:wrap;
      align-items:center;
      margin-top:6px;
    }

    .btn{
      display:inline-flex;
      align-items:center;
      gap:8px;
      padding:10px 14px;
      border-radius:10px;
      text-decoration:none;
      font-weight:700;
      cursor:pointer;
      font-size:14px;
    }

    .btn.primary{
      background:var(--success);
      color:white;
      border:0;
      box-shadow:0 10px 26px rgba(16,185,129,0.06);
    }

    .btn.ghost{
      background:transparent;
      color:var(--accent);
      border:1px solid rgba(43,125,233,0.12);
    }

    .note{
      display:block;
      margin-top:10px;
      font-size:13px;
      color:var(--muted);
    }

    /* Small screens */
    @media (max-width:560px){
      .success-card{flex-direction:column;align-items:center;text-align:center;padding:16px;gap:12px}
      .icon{width:64px;height:64px;min-width:0;border-radius:12px}
      .actions{justify-content:center}
    }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="success-card" role="status" aria-labelledby="succ-title">
      <div class="icon" aria-hidden="true">✓</div>

      <div class="content">
        <h1 id="succ-title">Action successful</h1>
        <p class="message">Your change was saved. The patient record has been updated successfully.</p>

        <div class="actions">
          <a class="btn primary" href="/home" title="Go to home">Home</a>
          <a class="btn ghost" href="/list" title="View list">List</a>
        </div>

        <span class="note">You can continue managing records or return to the homepage.</span>
      </div>
    </div>
  </div>
</body>
</html>