<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Login failed</title>
  <style>
    :root{
      --bg: #f6f8fb;
      --card: #ffffff;
      --danger: #ef4444;        /* red */
      --accent: #2b7de9;       /* blue */
      --muted: #6b7280;
      --radius: 12px;
      --shadow: 0 10px 30px rgba(15,23,42,0.06);
      font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    }

    /* Page layout */
    html,body{height:100%;margin:0;background:linear-gradient(180deg,var(--bg),#eef6ff);color:#0f172a}
    .wrap{
      min-height:100vh;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:28px 20px;
    }

    /* Card */
    .error-card{
      background:linear-gradient(180deg,var(--card),#fbfdff);
      border-radius:var(--radius);
      box-shadow:var(--shadow);
      padding:24px;
      max-width:520px;
      width:100%;
      border:1px solid rgba(15,23,42,0.04);
      display:flex;
      gap:18px;
      align-items:flex-start;
    }

    /* Icon column */
    .icon {
      width:64px;
      height:64px;
      min-width:64px;
      border-radius:10px;
      background:linear-gradient(180deg, rgba(239,68,68,0.12), rgba(239,68,68,0.06));
      display:flex;
      align-items:center;
      justify-content:center;
      color:var(--danger);
      font-size:28px;
      font-weight:800;
      box-shadow:0 8px 18px rgba(239,68,68,0.06);
    }

    /* Content column */
    .content{
      flex:1;
    }

    h1{
      margin:0 0 6px 0;
      font-size:20px;
      letter-spacing:-0.2px;
    }

    p.message{
      margin:0 0 14px 0;
      color:var(--muted);
      font-size:14px;
      line-height:1.5;
    }

    /* Actions */
    .actions{
      display:flex;
      gap:10px;
      flex-wrap:wrap;
      align-items:center;
    }

    a.btn{
      display:inline-flex;
      align-items:center;
      gap:8px;
      padding:10px 14px;
      border-radius:10px;
      text-decoration:none;
      font-weight:700;
      cursor:pointer;
    }

    .btn.primary{
      background:var(--accent);
      color:white;
      border:0;
      box-shadow:0 8px 22px rgba(43,125,233,0.08);
    }

    .btn.ghost{
      background:transparent;
      color:var(--accent);
      border:1px solid rgba(43,125,233,0.12);
    }

    .hint{
      display:block;
      margin-top:10px;
      font-size:13px;
      color:var(--muted);
    }

    /* Small screens */
    @media (max-width:520px){
      .error-card{flex-direction:column;align-items:center;text-align:center;padding:18px;gap:12px}
      .icon{width:56px;height:56px;min-width:0;border-radius:9px}
      .content{width:100%}
      .actions{justify-content:center}
    }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="error-card" role="alert" aria-labelledby="err-title">
      <div class="icon" aria-hidden="true">!</div>

      <div class="content">
        <h1 id="err-title">Login failed</h1>
        <p class="message">We couldn't sign you in. Please check your email and password and try again.</p>

        <div class="actions">
          <a class="btn primary" href="/login">Try again</a>
          <a class="btn ghost" href="/home">Back to home</a>
        </div>

        <span class="hint">If you forgot your password you can reset it from the login page.</span>
      </div>
    </div>
  </div>
</body>
</html>